# ---- Hugging Face ----
HF_TOKEN=hf_xxx_your_token_here
# Comma-separated list of repos (models or datasets). Examples:
HF_REPOS=sentence-transformers/all-MiniLM-L6-v2,openai/whisper-base

# Optional: pin revisions/commits (comma-separated, matched by index to HF_REPOS)
# Leave blank to let the sync pull latest default branch.
HF_REVISIONS=

# License allowlist (comma-separated SPDX or HF labels)
LICENSE_ALLOWLIST=apache-2.0,mit,bsd-3-clause,cc-by-4.0

# Optionally denylist specific repos or files (comma-separated regex)
REPO_DENYLIST=
FILE_DENYLIST=

# ---- MinIO / S3 target ----
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET=models
MINIO_PREFIX=mirrors/hf
MINIO_REGION=us-east-1
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false

# ---- Networking (optional, org-approved only) ----
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,minio,minio:9000,.svc,.cluster.local
